import { PerformanceReview } from './performance-review.model';

describe('PerformanceReview', () => {
  it('should create an instance', () => {
    expect(new PerformanceReview()).toBeTruthy();
  });
});
