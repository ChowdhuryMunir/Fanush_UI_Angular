import { PayrollIntegration } from './payroll-integration.model';

describe('PayrollIntegration', () => {
  it('should create an instance', () => {
    expect(new PayrollIntegration()).toBeTruthy();
  });
});
